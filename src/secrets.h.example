// ================================================================
//  secrets.h  —  COPY THIS FILE TO  src/secrets.h  AND FILL IT IN
// ================================================================
//  src/secrets.h is git-ignored so your credentials never get
//  committed. The build will fail until secrets.h exists.
// ================================================================
#pragma once

// ---- WiFi (station) ----
#define WIFI_SSID       "your-wifi-ssid"
#define WIFI_PASSWORD   "your-wifi-password"

// ---- WiFi fallback access point (when STA can't connect) ----
//  Connect to this AP, then browse to http://192.168.4.1/ for OTA.
#define AP_SSID         "Fridgectl Fallback Hotspot"
#define AP_PASSWORD     "sJpuPfsWNxdX"   // >= 8 chars

// ---- MQTT broker ----
#define MQTT_URI        "mqtt://192.168.1.10:1883"  // e.g. your HA/mosquitto host
#define MQTT_USERNAME   ""                          // "" if broker is anonymous
#define MQTT_PASSWORD   ""

// ---- HTTP OTA upload endpoint (Basic-Auth protected) ----
//  Browse to http://<device-ip>/  and upload a new firmware.bin
#define OTA_USERNAME    "admin"
#define OTA_PASSWORD    "change-me"
